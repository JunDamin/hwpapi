{
  "hash": "22c8e563575a66e55e722274f85e583a",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Collections\"\nsubtitle: \"One pattern for every list-of-things in a Document\"\n---\n\n# The pattern\n\nEvery document-scoped aggregation in hwpapi v2 follows **one**\ninterface — dict-like, iterable, sized, membership-testable,\nnames-exposing, and predicate-filterable. The contract lives in\n[`hwpapi.collections.Collection`](../reference/index.qmd) as a\n`typing.Protocol`:\n\n```python\nfrom typing import Callable, Iterator, Protocol, runtime_checkable\n\n@runtime_checkable\nclass Collection(Protocol):\n    def __getitem__(self, key): ...\n    def __iter__(self) -> Iterator: ...\n    def __len__(self) -> int: ...\n    def __contains__(self, key) -> bool: ...\n    def names(self) -> list[str]: ...\n    def filter(self, predicate: Callable[..., bool]) -> list: ...\n```\n\nEvery concrete collection under `app.doc.*` implements this:\n\n| `app.doc.*` | Returns | Elements keyed by |\n|---|---|---|\n| `fields` | `FieldCollection` | field name |\n| `bookmarks` | `BookmarkCollection` | bookmark name |\n| `hyperlinks` | `HyperlinkCollection` | URL or index |\n| `images` | `ImageCollection` | image name or index |\n| `styles` | `StyleCollection` | style name |\n| `paragraphs` | `ParagraphCollection` | index (`int`) |\n| `tables` | `TableCollection` | table name or index |\n\n# The common moves\n\nBecause every collection implements the same six methods, the same\nfive operations work on all of them:\n\n::: {#e1d16e52 .cell execution_count=1}\n``` {.python .cell-code}\ncoll = app.doc.fields                 # or bookmarks, images, ...\n\nlen(coll)                             # count\n\"author\" in coll                      # membership\nfor item in coll: ...                 # iterate\ncoll[\"author\"]                        # by key → element\ncoll.names()                          # list of keys\ncoll.filter(lambda f: \"head\" in f.name)   # list[Element] where predicate holds\n```\n:::\n\n\nCollections that support mutation add `__setitem__`, `__delitem__`,\nand `clear()`:\n\n::: {#eab68567 .cell execution_count=2}\n``` {.python .cell-code}\napp.doc.fields[\"author\"] = \"홍길동\"    # write\ndel app.doc.fields[\"author\"]          # remove one\napp.doc.fields.clear()                # remove all\n```\n:::\n\n\n# Element value objects\n\nSubscripting a collection returns a lightweight **element** object:\n\n- `fields[name]` → [`Field`](../reference/collections/fields.qmd) —\n  `.name`, `.value`, `.goto()`\n- `bookmarks[name]` → [`Bookmark`](../reference/collections/bookmarks.qmd) —\n  `.name`, `.goto()`\n- `hyperlinks[key]` → [`Hyperlink`](../reference/collections/hyperlinks.qmd)\n- `images[key]` → [`Image`](../reference/collections/images.qmd)\n- `paragraphs[i]` → [`Paragraph`](../reference/collections/paragraphs.qmd) —\n  `.text`, `.style`, `.charshape`, `.parashape`, `.runs`\n- `styles[name]` → [`Style`](../reference/collections/styles.qmd)\n- `tables[key]` → [`Table`](../reference/collections/tables.qmd) —\n  `.cell(row, col)`, `.rows`, `.cols`\n\nElements are *value objects*: constructing one does not touch COM.\nReading a property like `field.value` or `paragraph.text` is what\ntriggers the COM call. This matters when you iterate —\n`for p in app.doc.paragraphs: p.text` pays one COM hit per `text`\nread, not one per construction.\n\n# Recipes\n\n## Bulk-fill form fields from a dict\n\n::: {#84341339 .cell execution_count=3}\n``` {.python .cell-code}\ndata = {\"author\": \"홍길동\", \"date\": \"2026-04-19\", \"title\": \"보고서\"}\n\nfor name, value in data.items():\n    if name in app.doc.fields:\n        app.doc.fields[name] = value\n```\n:::\n\n\n## Find all paragraphs that use a specific style\n\n::: {#9cbd807e .cell execution_count=4}\n``` {.python .cell-code}\npara = app.doc.paragraphs.filter(lambda p: p.style == \"Heading 1\")\nprint(f\"{len(para)} heading 1 paragraphs\")\n```\n:::\n\n\n## Rename a bookmark\n\n::: {#72a2d071 .cell execution_count=5}\n``` {.python .cell-code}\nif \"old-name\" in app.doc.bookmarks:\n    app.doc.bookmarks.rename(\"old-name\", \"new-name\")\n```\n:::\n\n\n## List every image in the document\n\n::: {#423bc0c0 .cell execution_count=6}\n``` {.python .cell-code}\nfor img in app.doc.images:\n    print(img.name, img.width, img.height)\n```\n:::\n\n\n# Migration note\n\nIn v1, these lived on `App` directly:\n\n| v1 | v2 |\n|---|---|\n| `app.fields` | `app.doc.fields` |\n| `app.bookmarks` | `app.doc.bookmarks` |\n| `app.hyperlinks` | `app.doc.hyperlinks` |\n| `app.images` | `app.doc.images` |\n| `app.styles` | `app.doc.styles` |\n| `app.field_names` | `app.doc.fields.names()` |\n| `app.field_exists(n)` | `n in app.doc.fields` |\n| `app.delete_field(n)` | `del app.doc.fields[n]` |\n| `app.delete_all_fields()` | `app.doc.fields.clear()` |\n\nSee the [migration guide](../getting-started/migration-v1-to-v2.qmd) for\nthe complete table.\n\n",
    "supporting": [
      "collections_files"
    ],
    "filters": [],
    "includes": {}
  }
}