{
  "hash": "0a028cc9c8d95ecd3e5ed5cedc787c10",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"글자 서식\"\nsubtitle: \"굵게, 색상, 크기 — 스코프와 ParameterSet 을 통해\"\n---\n\n`examples/charshape_bold.py` 에서 이식.\n\n# 1. 스코프 기반 — 일반적인 경우\n\n::: {#a812eb53 .cell execution_count=1}\n``` {.python .cell-code}\nfrom hwpapi import App\nfrom hwpapi.context import charshape_scope\nfrom hwpapi import units as U\n\nwith App() as app:\n    app.new()\n\n    with charshape_scope(app, bold=True, size=U.pt(14), color=\"#C0392B\"):\n        app.doc.insert_text(\"This line is bold 14pt red\\n\")\n    # 여기서 커서 스타일이 복원됨\n    app.doc.insert_text(\"Back to default\\n\")\n```\n:::\n\n\n# 2. 요소 기반 — 특정 문단에 대해\n\n::: {#1b28fdba .cell execution_count=2}\n``` {.python .cell-code}\nfrom hwpapi import App\nfrom hwpapi.low.parametersets import CharShape\n\nwith App() as app:\n    app.open(\"report.hwp\")\n\n    para0 = app.doc.paragraphs[0]\n    para0.charshape = CharShape(Bold=True, Height=1800, TextColor=\"#000080\")\n```\n:::\n\n\n# 3. 단일 속성 변경\n\n::: {#7cd91ebe .cell execution_count=3}\n``` {.python .cell-code}\nfrom hwpapi import App\n\nwith App() as app:\n    app.open(\"report.hwp\")\n\n    # 현재 모양을 읽고, 한 키만 바꿔서 다시 쓰기\n    cs = app.doc.paragraphs[0].charshape\n    cs.Italic = True\n    app.doc.paragraphs[0].charshape = cs\n```\n:::\n\n\n# 4. clone 을 이용한 스냅샷 / 복원\n\n::: {#e560ab55 .cell execution_count=4}\n``` {.python .cell-code}\nfrom hwpapi import App\n\nwith App() as app:\n    app.new()\n    app.doc.insert_text(\"Snapshot test.\\n\")\n    app.doc.select_all()\n\n    action = app.actions.CharShape\n    ps = action.pset\n\n    # 스냅샷 — 네이티브 pset.Clone()\n    original = ps.clone()\n    print(f\"Snapshot: Bold={original.Bold}, Italic={original.Italic}\")\n\n    # 임시 변경 적용\n    ps.Bold = True\n    ps.Italic = True\n    ps.TextColor = \"#00FF00\"\n    action.run()\n    print(\"Applied temporary formatting.\")\n\n    # 스냅샷에서 복원\n    ps.update_from(original)\n    action.run()\n    print(\"Restored original formatting.\")\n```\n:::\n\n\n# 5. 실행 없이 인트로스펙트\n\n::: {#07d74fef .cell execution_count=5}\n``` {.python .cell-code}\nfrom hwpapi import App\n\nwith App() as app:\n    cls = app.actions.get_pset_class(\"CharShape\")\n    desc = app.actions.get_description(\"CharShape\")\n    print(f\"CharShape → {cls.__name__}: {desc}\")\n    print(f\"Has {len(cls._property_registry)} properties\")\n    print(f\"First 5: {list(cls._property_registry.keys())[:5]}\")\n```\n:::\n\n\n# 함께 보기\n\n- [Guide: context scopes](../guide/context-scopes.qmd)\n- [Reference: context.scopes](../../reference/context/scopes.html)\n- [Reference: low.parametersets](../../reference/low/parametersets.html)\n\n",
    "supporting": [
      "charshape-formatting_files"
    ],
    "filters": [],
    "includes": {}
  }
}